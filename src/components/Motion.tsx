'use client'; import {useEffect} from 'react'; import gsap from 'gsap'; import {ScrollTrigger} from 'gsap/ScrollTrigger';
export default function Motion(){useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const loader=document.getElementById('loader');const intro=gsap.timeline();intro.to(loader,{opacity:0,duration:.65,delay:.35,ease:'power2.out',onComplete:()=>loader?.classList.add('hide')});gsap.utils.toArray<HTMLElement>('.reveal').forEach((el)=>gsap.fromTo(el,{y:45,opacity:0},{y:0,opacity:1,duration:1,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 86%'}}));intro.fromTo('.hero h1',{y:70,opacity:0},{y:0,opacity:1,duration:1.25,ease:'power4.out'},'-=.15');intro.fromTo('.hero-content>p',{y:25,opacity:0},{y:0,opacity:1,duration:.8,ease:'power3.out'},'-=.75');gsap.to('.hero-art',{scale:1.08,scrollTrigger:{trigger:'.hero',start:'top top',end:'bottom top',scrub:1}});return()=>ScrollTrigger.getAll().forEach(t=>t.kill())},[]);return null}
